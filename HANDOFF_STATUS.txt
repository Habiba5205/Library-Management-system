Library Management System MVC - Handoff Status

Current stopping point:
Full catalog, user management, and borrowing flow are complete: Categories,
Authors, Books (with category/author selection, search, filtering), Users
(with real password hashing), Borrowings (borrow/return/history), and Payments
CRUD. Dashboard and basic UI polish are complete. Optional authentication/login
is the main remaining bonus feature.

Finished work:
1. Created an ASP.NET Core MVC project.
2. Connected the project to the GitHub repository:
   https://github.com/Habiba5205/Library-Management-system.git
3. Created the main models from the ERD:
   - Role
   - User
   - Book
   - Author
   - Category
   - BookAuthor
   - Borrowing
   - Payment
4. Installed Entity Framework Core packages:
   - Microsoft.EntityFrameworkCore.SqlServer
   - Microsoft.EntityFrameworkCore.Tools
5. Created ApplicationDbContext in the Data folder.
6. Added DbSet properties for all models.
7. Configured database relationships:
   - Role to Users
   - Category to Books
   - User/Manager to Books
   - Book to Author using BookAuthor
   - User to Borrowings
   - Book to Borrowings
   - Borrowing to Payments
8. Added unique indexes for:
   - RoleName
   - Email
   - Username
   - ISBN
9. Added decimal precision for:
   - Book.Price
   - Payment.Amount
10. Added seed data for roles:
   - Admin
   - Manager
   - Member
11. Added SQL Server connection string in appsettings.json.
12. Created the first EF Core migration:
   InitialCreate
13. Created the local database:
   LibSystemMvcDb
14. Added NuGet.config so packages restore into the project-local .nuget-packages folder.
15. Verified the project builds successfully with 0 errors and 0 warnings.
16. Built CategoryController with full CRUD (Index, Details, Create, Edit, Delete).
17. Built matching Razor views under Views/Category.
18. Category delete blocks removal if the category still has books
    assigned to it (Book.CategoryId, Restrict on delete).
19. Built AuthorController with full CRUD (Index, Details, Create, Edit, Delete).
20. Built matching Razor views under Views/Author.
21. Author Details page shows all books linked via BookAuthor.
22. Author delete warns (but does not block) if the author has linked
    books, since BookAuthor -> Author cascades on delete: it removes
    the link rows only, not the books themselves.
23. Built BookController with full CRUD (Index, Details, Create, Edit, Delete).
24. Added a BookFormViewModel (ViewModels folder) to handle the Category
    dropdown, optional Manager dropdown, and Author multi-select checkboxes
    on Create/Edit, since these can't bind directly to the Book entity.
25. Book Index supports search by title/ISBN and filtering by category
    and author.
26. Manager dropdown is scoped to Users where Role.RoleName == "Manager".
27. AvailabilityStatus uses a fixed dropdown: Available, Borrowed,
    Reserved, Unavailable (not defined by the ERD, agreed as a working set).
28. Book delete is blocked if the book has borrowing history
    (Borrowing.BookId, Restrict on delete) - Delete button disables
    and shows a message instead.
29. Built UserController with full CRUD (Index, Details, Create, Edit, Delete).
30. Added UserFormViewModel (ViewModels folder) with a plaintext Password
    field, separate from the stored PasswordHash.
31. Passwords are hashed with PasswordHasher<User> (Microsoft.AspNetCore.Identity,
    already available via the Web SDK, no extra NuGet needed). Plaintext
    password is never stored - only the hash. Verified in SQL Server Object
    Explorer that PasswordHash is a long hashed string, not plaintext.
32. Password is required on Create; optional on Edit (blank = keep current
    password).
33. User delete is blocked if the user has borrowing history
    (Borrowing.UserId, Restrict on delete). If the user only manages books,
    delete is allowed and Book.ManagerId is set to null automatically
    (SetNull on delete).
34. Added "Categories", "Authors", "Books", and "Users" links to the
    navbar in _Layout.cshtml.
35. Tested locally: Category, Author, Book, and User CRUD all confirmed
    working end-to-end (create, view, edit, delete, search, filter,
    password hashing).
36. Built BorrowingController with Index, Details, Create, and Return actions.
37. Added BorrowingFormViewModel for selecting an available book, selecting
    a member, setting the borrow date, and calculating the loan period.
38. Built matching Razor views under Views/Borrowing.
39. Borrowing Create blocks unavailable books and only accepts users with the
    Member role.
40. Borrowing Create sets the borrowing status to Borrowed, calculates DueDate,
    and changes Book.AvailabilityStatus to Borrowed.
41. Return flow sets ReturnDate, changes borrowing status to Returned, and
    changes Book.AvailabilityStatus back to Available.
42. Added "Borrowings" link to the navbar.
43. Smoke-tested Borrowing Index and Create pages locally. Both returned 200 OK.
44. Built PaymentController with full CRUD (Index, Details, Create, Edit, Delete).
45. Added PaymentFormViewModel for selecting a borrowing record and entering
    amount, payment date, method, status, and transaction reference.
46. Built matching Razor views under Views/Payment.
47. Payment Index supports filtering by status.
48. Payment Create/Edit validates that the selected borrowing record exists.
49. Added "Payments" link to the navbar.
50. Added an "Add Payment" shortcut from Borrowing Details.
51. Replaced the default ASP.NET home page with a dashboard.
52. Dashboard shows book count, available books, member count, active
    borrowings, overdue borrowings, payment count, total paid amount,
    recent borrowings, and recent payments.
53. Updated the app name in the layout from Lib_System to Library Management.
54. Cleaned the main navbar and footer.
55. Added basic dashboard/layout styling in wwwroot/css/site.css.

Database location:
Use SQL Server Object Explorer or SQL Server Management Studio.

Server:
(localdb)\MSSQLLocalDB

Database:
LibSystemMvcDb

Expected tables:
- Authors
- Categories
- Roles
- Users
- Books
- BookAuthors
- Borrowings
- Payments
- __EFMigrationsHistory

Useful commands for teammate after pulling:
dotnet restore
dotnet tool restore
dotnet tool run dotnet-ef database update
dotnet build Lib_System.csproj

Next step:
Optional: add authentication/login and role-based access.

Recommended order:
1. Categories CRUD -- DONE
2. Authors CRUD -- DONE
3. Books CRUD with category and author selection -- DONE
4. Users or Members CRUD -- DONE
5. Borrowing and return flow -- DONE
6. Payments -- DONE
7. Navbar and UI polish -- DONE
8. Authentication/login -- OPTIONAL BONUS

Important note:
The API specification was converted into an MVC project direction. We are not building API endpoints right now. The next work should use Controllers and Razor Views, not Web API controllers.

Known gaps / things to watch:
- No authentication/login exists yet. Password hashing is in place, but
  there's no login page, session, or cookie auth wired up - a user's
  password is stored securely but nothing checks it against anything yet.
  This may be needed if the instructor requires login, per the original
  spec's "optional bonus" auth requirement.
- Plain `dotnet build` may ask for a project/solution choice because the
  folder now contains both a project file and a solution file. Use
  `dotnet build Lib_System.csproj`.
