Library Management System MVC - Handoff Status

Current stopping point:
The project foundation and database setup are finished. The next person should start building the MVC CRUD pages.

Finished work:
1. Created an ASP.NET Core MVC project.
2. Connected the project to the GitHub repository:
   https://github.com/Habiba5205/Library-Management-system.git
3. Created the main models from the ERD:
   - Role
   - User
   - Book
   - Author
   - Category
   - BookAuthor
   - Borrowing
   - Payment
4. Installed Entity Framework Core packages:
   - Microsoft.EntityFrameworkCore.SqlServer
   - Microsoft.EntityFrameworkCore.Tools
5. Created ApplicationDbContext in the Data folder.
6. Added DbSet properties for all models.
7. Configured database relationships:
   - Role to Users
   - Category to Books
   - User/Manager to Books
   - Book to Author using BookAuthor
   - User to Borrowings
   - Book to Borrowings
   - Borrowing to Payments
8. Added unique indexes for:
   - RoleName
   - Email
   - Username
   - ISBN
9. Added decimal precision for:
   - Book.Price
   - Payment.Amount
10. Added seed data for roles:
   - Admin
   - Manager
   - Member
11. Added SQL Server connection string in appsettings.json.
12. Created the first EF Core migration:
   InitialCreate
13. Created the local database:
   LibSystemMvcDb
14. Added NuGet.config so packages restore into the project-local .nuget-packages folder.
15. Verified the project builds successfully with 0 errors and 0 warnings.
16. Built CategoryController with full CRUD (Index, Details, Create, Edit, Delete).
17. Built matching Razor views under Views/Category.
18. Category delete blocks removal if the category still has books
    assigned to it (Book.CategoryId, Restrict on delete).
19. Built AuthorController with full CRUD (Index, Details, Create, Edit, Delete).
20. Built matching Razor views under Views/Author.
21. Author Details page shows all books linked via BookAuthor.
22. Author delete warns (but does not block) if the author has linked
    books, since BookAuthor -> Author cascades on delete: it removes
    the link rows only, not the books themselves.
23. Added "Categories" and "Authors" links to the navbar in _Layout.cshtml.
24. Tested locally: Category and Author CRUD both confirmed working
    end-to-end (create, view, edit, delete).

Database location:
Use SQL Server Object Explorer or SQL Server Management Studio.

Server:
(localdb)\MSSQLLocalDB

Database:
LibSystemMvcDb

Expected tables:
- Authors
- Categories
- Roles
- Users
- Books
- BookAuthors
- Borrowings
- Payments
- __EFMigrationsHistory

Useful commands for teammate after pulling:
dotnet restore
dotnet tool restore
dotnet tool run dotnet-ef database update
dotnet build

Next step:
Start building Books CRUD pages.

Recommended order:
1. Categories CRUD -- DONE
2. Authors CRUD -- DONE
3. Books CRUD with category and author selection -- IN PROGRESS
   - Category dropdown (single select, FK)
   - Author multi-select (many-to-many via BookAuthor)
   - Manager dropdown (optional, FK to User)
   - Search by title/ISBN, filter by category/author
   - AvailabilityStatus display
4. Users or Members CRUD
5. Borrowing and return flow
6. Payments
7. Navbar and UI polish

Important note:
The API specification was converted into an MVC project direction. We are not building API endpoints right now. The next work should use Controllers and Razor Views, not Web API controllers.

---

Update: Category CRUD

Finished:
1. Built CategoryController with full CRUD (Index, Details, Create, Edit, Delete).
2. Built matching Razor views under Views/Category.
3. Delete action prevents removing a category that still has books
   assigned to it (checks Book.CategoryId before deleting).
4. Added "Categories" link to the navbar in _Layout.cshtml.
5. Created the local database on second dev machine using the existing
   InitialCreate migration (dotnet tool run dotnet-ef database update).
6. Tested full flow locally: create category, view details, edit, delete.
   All working as expected.

Next step:
Authors CRUD (same pattern as Categories), then Books CRUD with
category/author selection.

---

Update: Author CRUD

Finished:
1. Built AuthorController with full CRUD (Index, Details, Create, Edit, Delete).
2. Built matching Razor views under Views/Author.
3. Details page shows all books linked to the author via BookAuthor.
4. Delete page warns if the author has linked books (lists them), since
   BookAuthor -> Author cascades on delete: deleting an author removes
   the BookAuthor link rows but does NOT delete the books themselves.
   Unlike Category, this delete is not blocked, only flagged.
5. Added "Authors" link to the navbar in _Layout.cshtml.
6. Tested locally: create, view details, edit, delete all confirmed working.
