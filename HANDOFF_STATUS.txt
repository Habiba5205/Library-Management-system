Library Management System MVC - Handoff Status

Current stopping point:
Full catalog CRUD complete: Categories, Authors, and Books (with category/author 
selection, search, and filtering). Users/Members CRUD is next.

Finished work:
1. Created an ASP.NET Core MVC project.
2. Connected the project to the GitHub repository:
   https://github.com/Habiba5205/Library-Management-system.git
3. Created the main models from the ERD:
   - Role
   - User
   - Book
   - Author
   - Category
   - BookAuthor
   - Borrowing
   - Payment
4. Installed Entity Framework Core packages:
   - Microsoft.EntityFrameworkCore.SqlServer
   - Microsoft.EntityFrameworkCore.Tools
5. Created ApplicationDbContext in the Data folder.
6. Added DbSet properties for all models.
7. Configured database relationships:
   - Role to Users
   - Category to Books
   - User/Manager to Books
   - Book to Author using BookAuthor
   - User to Borrowings
   - Book to Borrowings
   - Borrowing to Payments
8. Added unique indexes for:
   - RoleName
   - Email
   - Username
   - ISBN
9. Added decimal precision for:
   - Book.Price
   - Payment.Amount
10. Added seed data for roles:
   - Admin
   - Manager
   - Member
11. Added SQL Server connection string in appsettings.json.
12. Created the first EF Core migration:
   InitialCreate
13. Created the local database:
   LibSystemMvcDb
14. Added NuGet.config so packages restore into the project-local .nuget-packages folder.
15. Verified the project builds successfully with 0 errors and 0 warnings.
16. Built CategoryController with full CRUD (Index, Details, Create, Edit, Delete).
17. Built matching Razor views under Views/Category.
18. Category delete blocks removal if the category still has books
    assigned to it (Book.CategoryId, Restrict on delete).
19. Built AuthorController with full CRUD (Index, Details, Create, Edit, Delete).
20. Built matching Razor views under Views/Author.
21. Author Details page shows all books linked via BookAuthor.
22. Author delete warns (but does not block) if the author has linked
    books, since BookAuthor -> Author cascades on delete: it removes
    the link rows only, not the books themselves.
23. Built BookController with full CRUD (Index, Details, Create, Edit, Delete).
24. Added a BookFormViewModel (ViewModels folder) to handle the Category
    dropdown, optional Manager dropdown, and Author multi-select checkboxes
    on Create/Edit, since these can't bind directly to the Book entity.
25. Book Index supports search by title/ISBN and filtering by category
    and author.
26. Manager dropdown is scoped to Users where Role.RoleName == "Manager".
27. AvailabilityStatus uses a fixed dropdown: Available, Borrowed,
    Reserved, Unavailable (not defined by the ERD, agreed as a working set).
28. Book delete is blocked if the book has borrowing history
    (Borrowing.BookId, Restrict on delete) - Delete button disables
    and shows a message instead.
29. Added "Categories", "Authors", and "Books" links to the navbar in
    _Layout.cshtml.
30. Tested locally: Category, Author, and Book CRUD all confirmed working
    end-to-end (create, view, edit, delete, search, filter).

Database location:
Use SQL Server Object Explorer or SQL Server Management Studio.

Server:
(localdb)\MSSQLLocalDB

Database:
LibSystemMvcDb

Expected tables:
- Authors
- Categories
- Roles
- Users
- Books
- BookAuthors
- Borrowings
- Payments
- __EFMigrationsHistory

Useful commands for teammate after pulling:
dotnet restore
dotnet tool restore
dotnet tool run dotnet-ef database update
dotnet build

Next step:
Start building Users/Members CRUD.

Recommended order:
1. Categories CRUD -- DONE
2. Authors CRUD -- DONE
3. Books CRUD with category and author selection -- DONE
4. Users or Members CRUD -- IN PROGRESS
5. Borrowing and return flow
6. Payments
7. Navbar and UI polish

Important note:
The API specification was converted into an MVC project direction. We are not building API endpoints right now. The next work should use Controllers and Razor Views, not Web API controllers.

Known gaps / things to watch:
- No Users exist in the database yet, so the Manager dropdown on Books
  is currently empty. Once Users CRUD exists and a Manager-role user is
  added, go back and re-test the Book Create/Edit Manager dropdown.
- PasswordHash on User is a plain string field for now - if login/auth
  gets added later, this needs real password hashing (not just a
  plaintext string being called "PasswordHash").